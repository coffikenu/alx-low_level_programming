#include "main.h"
#include <stdio.h>
#include <string.h>
void _print_rev_recursion(char *s)
{
int j;
for (j = strlen(s); j >= 1; j--)
{
if (j > 1)
{
printf("%c", s[j]);
}
else if (j == 1)
{
printf("%c%c", s[j], s[j - 1]);
}
}
}
