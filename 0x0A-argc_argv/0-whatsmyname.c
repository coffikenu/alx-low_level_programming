#include <stdio.h>
/**
 * main - is main method
 * @argc: is parametr
 * @argv: is second parametr
 * return - Always return 0
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[argc - 1]);
return (0);
}
