#include "main.h"
#include <stdio.h>
/**
 *main - print the multiple of two argument
 *@argc: first parametr
 *@argv: second parametr
 *Return: always zero
 */
int main(int argc, char *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
