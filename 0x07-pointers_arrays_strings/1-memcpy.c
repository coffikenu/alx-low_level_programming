#include "main.h"
#include <string.h>
/**
 * _memcpy - check the code
 * @dest: first parametr
 * @src: second parametr
 * @n: third parametr
 * Return: Always 0.
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
return (memcpy(dest, src, n));
}
