#include "main.h"
#include <string.h>
/**
 * _strcpy - check the code
 *@dest: is first parametr
 *@src: is second parametr
 * Return: Always 0.
 */
char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
