#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * puts2 - check the code
 * @str: is a parametr
 * Return: Always 0.
 */
void puts2(char *str)
{
int i;
for (i = 0; i < strlen(str); i++)
{
if(i % 2 == 0)
{
putchar(str[i]);
}
}
putchar('\n');
}
