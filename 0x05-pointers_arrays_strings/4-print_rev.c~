#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * print_rev - check the code
 * @s: is parametr
 * Return: Always 0.
 */
void print_rev(char *s)
{
int i;
char n[70];
 int j = 0;
for (i = strlen(s)-1; i >= 0; i--)
{
n[j] = s[i];
j++;
}
puts(n);
}
