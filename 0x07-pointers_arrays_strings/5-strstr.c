#include "main.h"
#include <string.h>
/**
 * _strstr - check the code
 * @haystack: first pararametr
 * @needle: second parametr
 * Return: Always 0.
 */
char *_strstr(char *haystack, char *needle)
{
return (strstr(haystack, needle));
}
