#include "main.h"
#include <string.h>
/**
 * _memset - check the code
 * @s: first parametr
 * @b: second parametr
 * @n: third parametr
 * Return: Always 0.
 */
char *_memset(char *s, char b, unsigned int n)
{
return (memset(s, b, n));
}
