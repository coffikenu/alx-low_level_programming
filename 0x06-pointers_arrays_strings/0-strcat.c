#include "main.h"
#include <string.h>
/**
 * _strcat - check the code
 * @dest: first parametr
 * @src: second parametr
 * Return: Always 0.
 */
char *_strcat(char *dest, char *src)
{
dest = strcat(dest, src);
return (dest);
}
